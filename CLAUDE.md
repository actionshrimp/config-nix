# Applying changes

First, build the update version to check it works as expected:

```bash
make darwin-build
```
If everything works as expected, you can apply the changes to the main branch:

```bash
make darwin-rebuild-switch
```
